apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-flask.fullname" . }}-test-connection"
  labels:
    {{- include "portal-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
